- 2026-08-15 | 断面図が平坦地でギザギザに描画される（繰り返し指摘・根本未解決だった） | 原因: 国土地理院DEMは①格子1m/5m/10m ②標高値が0.1m刻みに丸め済み、という二重の離散化がある。断面図は既定0.5m間隔＝DEM解像度より細かくサンプリングしており、同じ値が4〜8点連続→0.1mだけカクッと落ちる階段＋単発スパイク(例 15.2→16.2→15.9)が必然的に発生していた。地形が実際にギザギザなのではなく、データが情報を持たない解像度まで拡大表示していた artifact。実測で確認: 敷地内20m区間の生値ラン長 [21.9×4][21.8×8][21.7×1][21.6×7][21.5×2][21.6×1][21.5×3][21.4×5][21.3×6][21.2×3] = 実体は20mで0.7m下がる約2°の緩斜面 | 5a4e0f2 | #描画 @jissou グローバル:済(user-checkの型として汎用: 「データの実解像度より細かくサンプリングして描画すると、量子化の階段をノイズと誤認する」) ヨコテン:済1件(showStripSection経路にも同じhsrc検出を適用。平坦度は格子2.25m>DEM1mで過剰サンプリングしておらず、displayElevsも通らない=対象外と実測確認)
- 2026-08-15 | 断面図が中央に小さくレターボックス表示される（前々からの指摘） | 原因: .section固定312pxに対しsec-head/sec-readoutの実消費テキスト量が過去の追加で増え続け、chartの実高さが118.6pxまで縮小。drawChart()のviewBox高さがMath.max(180,rect.height)で最低180にクランプされアスペクト比が乖離し、SVGのpreserveAspectRatio既定で縦に合わせて縮小→横に大きな余白ができていた | 8c104a3 | #描画 @jissou グローバル:不要(この画面固有のレイアウト事情) ヨコテン:済0件(同種の固定height+可変テキストの組み合わせは他になし)
