- 2026-08-15 | 断面図が平坦地でギザギザに描画される（繰り返し指摘・根本未解決だった） | 原因: 国土地理院DEMは①格子1m/5m/10m ②標高値が0.1m刻みに丸め済み、という二重の離散化がある。断面図は既定0.5m間隔＝DEM解像度より細かくサンプリングしており、同じ値が4〜8点連続→0.1mだけカクッと落ちる階段＋単発スパイク(例 15.2→16.2→15.9)が必然的に発生していた。地形が実際にギザギザなのではなく、データが情報を持たない解像度まで拡大表示していた artifact。実測で確認: 敷地内20m区間の生値ラン長 [21.9×4][21.8×8][21.7×1][21.6×7][21.5×2][21.6×1][21.5×3][21.4×5][21.3×6][21.2×3] = 実体は20mで0.7m下がる約2°の緩斜面 | 5a4e0f2 | #描画 @jissou グローバル:済(user-checkの型として汎用: 「データの実解像度より細かくサンプリングして描画すると、量子化の階段をノイズと誤認する」) ヨコテン:済1件(showStripSection経路にも同じhsrc検出を適用。平坦度は格子2.25m>DEM1mで過剰サンプリングしておらず、displayElevsも通らない=対象外と実測確認)
- 2026-08-15 | 断面図が中央に小さくレターボックス表示される（前々からの指摘） | 原因: .section固定312pxに対しsec-head/sec-readoutの実消費テキスト量が過去の追加で増え続け、chartの実高さが118.6pxまで縮小。drawChart()のviewBox高さがMath.max(180,rect.height)で最低180にクランプされアスペクト比が乖離し、SVGのpreserveAspectRatio既定で縦に合わせて縮小→横に大きな余白ができていた | 8c104a3 | #描画 @jissou グローバル:不要(この画面固有のレイアウト事情) ヨコテン:済0件(同種の固定height+可変テキストの組み合わせは他になし)
- 2026-08-16 | (設計ミス・実装前に訂正) 平坦度の切土/盛土対応で「使える面積」の基準を新旧方式で固定値に切り離す設計を提案したが、ユーザーから「なぜ方式が変わると求め方が変わるのか、累積で段階的に求めるだけでは」と指摘を受けた | 原因: bestUsableAreaForCells/bestRefForRegionは元々符号なしの±window判定で新旧方式どちらでも変更不要だったのに、不要な抽象化(スキーム非依存の固定値という概念)を導入しようとした。実装前のユーザーレビューで発覚し、着手前に設計を修正(currentUsableM()で呼び出し側のしきい値だけを切替に変更) | d3bcfda | #設計 @sekkei グローバル:不要(この機能固有の設計判断) ヨコテン:済0件(実装前に是正したためコードへの影響なし)
- 2026-08-16 | 切土/盛土の色を単一系統に変更する改修中、TypeError(Cannot read properties of undefined)でgenerateFlat()が停止 | 原因: CUTFILL_COLORSを{cut:[],fill:[]}から単一配列に変更した際、cutFillTableHTML内の差引土量表示が旧構造のCUTFILL_COLORS.cut[2]/.fill[2]参照のまま残っていた(grep漏れ)。実装直後のブラウザ検証で即発覚 | 45a8edd | #実装 @jissou グローバル:不要(単純な参照漏れ) ヨコテン:済1件(grep -n "CUTFILL_COLORS\.\(cut\|fill\)"で全箇所を棚卸しし、他に残存参照がないことを確認)
- 2026-08-16 | 平坦度(新方式)の切土/盛土バッジが基準ラインの区間(path)ごとに配置され、意図した1組でなく2組(4個)出ていた | 原因: drawCutFillBadges()をdrawFlatContours()のpaths.forEach内で呼んでおり、dh=0の等高線が複数の分離した区間に分かれる敷地では区間の数だけバッジ対が生成されていた。ユーザー指摘で発覚 | 283a655 | #実装 @jissou グローバル:不要(この機能固有) ヨコテン:済0件(バッジ生成箇所はこの1関数のみ)
- 2026-08-16 | 平坦度(新方式)の切土/盛土バッジが変な位置に出る | 原因: バッジ配置がdh=0等高線の最長区間の中点における「隣接2点だけの接線」から法線方向を求め、格子1〜2マス分(3〜4m)のオフセットで高低を判定していた。marching squaresの等高線はDEMノイズで局所的に折れ曲がりやすく、隣接2点だけの接線は不安定で、境界ギリギリの位置に着地したり誤判定することがあった。ユーザー指摘で発覚(「制御難しければ機能撤回も可」との申し出を受けたが、より単純で頑丈な代替方式があったため撤回せず修正) | 892808d,283a655(旧実装)→20b5ec5(修正) | #実装 @jissou グローバル:済(局所的な等高線接線から向きを推定する設計は本質的にノイズに弱い。連結領域の重心のような大域的な統計量を使う方が頑丈、という教訓は他プロジェクトの方向表示にも汎用) ヨコテン:済0件(バッジ配置ロジックはこの1関数のみ)
- 2026-08-16 | DE分割パネルの「この使える面積になる%へ」ボタンが折り返されレイアウトが崩れる | 原因: labelとbuttonをflex+flex-wrapで並べており、右ペイン幅400pxに対しボタン文言が長すぎて折り返され、目標面積行と目標使える面積行で入力欄・ボタンの縦位置が揃っていなかった | 6f358ff | #描画 @jissou グローバル:不要(この画面固有) ヨコテン:済0件(該当箇所はこの2行のみ)
- 2026-08-17 | 価格シミュレーション機能を「基準価格1298万円=敷地全体の価格」という前提で実装したが、実際は「A区画だけの既知価格」で、B/C/全体の価格は誰も知らない、という前提を取り違えていた | 原因: ユーザー発言「999.9㎡の比率から」を、金額計算ルール(完全委任の境界①に該当)であるにも関わらずAskUserQuestionで確定させず、「基準価格・基準面積を自由入力にすれば解釈違いを吸収できる」という設計で押し切った。999.9㎡が実際にはA区画の実面積そのものだったため、依存関係の向き(A→他区画を導出、ではなく全体→各区画で按分)を丸ごと取り違えた | da3f839(誤)→b7e83ad(修正) | #実装 @jissou グローバル:不要(既存の金銭計算ルール確認(完全委任の境界①)の運用不足であり、ルール自体は既存。今後は①に該当する時、自由入力で吸収できそうに見えても曖昧な依存関係の向きはAskUserQuestionで確定させてから実装する) ヨコテン:済0件(価格計算はこの1箇所のみ)
- 2026-08-17 | 物件を切り替えても崖の測線(POINTS.top/bottom)が前物件の座標に残ったまま表示され、下端が離れた変な場所に固定されているように見えた | 原因: switchSite()がPOINTS.top/bottomを一切リセットしておらず、hannanの初期ハードコード座標か直前に見ていた物件の座標がそのまま残っていた。ユーザー指摘で発覚 | 6a4b72a | #実装 @jissou グローバル:不要(このアプリの物件切替固有) ヨコテン:済1件(同じ「switchSite()がリセットし忘れる」機構でpicks/SAMPLES/confirmedCliffsも前物件のデータが残っていたことを棚卸しで発見・同じコミットで修正)
- 2026-08-17 | #186修正直後、Ctrl+F5直後(初期状態・未操作)でも「✅両方の点を置きました」と表示される | 原因: POINTS初期値のplacedを、hannanの座標が最初から作図済みだから、という理由でtrueにしていたが、「座標が入っている」と「今のユーザーが実際に置いた」を混同していた。ユーザー実機確認で発覚 | 621af38 | #実装 @jissou グローバル:不要(この機能固有) ヨコテン:済0件(placed初期値はこの1箇所のみ)
- 2026-08-17 | #186再修正後も、片方の点を置いた直後にもう片方(未配置)の仮位置へ線が伸びて「変な点」に見える | 原因: drawMeasure()が未配置点も薄い透明度で描画し続けており、透明度を下げても「離れた仮位置への線」自体は消えていなかった。ユーザー実機確認で発覚 | 6163fbb | #実装 @jissou グローバル:不要(この機能固有) ヨコテン:済0件(未配置点の描画分岐はdrawMeasure()のみ)
- 2026-08-17 | 断面図で任意の2点を選ぶと、がけの基準(H>2m かつ勾配>30°)を満たさない緩斜面でも地図に赤い「2H影響ライン到達点（要対策範囲端）」が描かれた。右パネルは「がけに該当しない可能性」と正しく出ており、地図と文言が矛盾していた | 原因: 判定式 isCliff=H>thH&&ang>thA が updateResults() の文言生成側にしか無く、地図描画の drawCliff()／確定の confirmCliff() は getCliff() が返す2点から無条件に2H線を描いていた。同じ判定の実装が1箇所に集約されていなかった(知識の二重管理)。ユーザー実機で発覚 | 8f2b9dc | #実装 @jissou グローバル:済(「同じ判定ロジックを表示系と描画系で二重に持たない＝判定関数を1本に切り出して両方から呼ぶ」。表示と実体が食い違うと、誤った資料が外部に出る事故になる) ヨコテン:済2件(drawCliff／confirmCliff の2経路を isCliffPair() に集約。confirmedCliffsの再描画・断面図SVG側の2H表示も棚卸ししたが、SVG側は選択2点の勾配を数値表示するだけで「要対策範囲」と断定する表現は無く対象外と判断)
- 2026-08-17 | 平坦度の面積が実測図とずれる（敷地全体2884㎡と表示、実測図は2876.5㎡）。交渉資料として実測図と突き合わせられると数字が食い違う | 原因: 面積を格子セルの数え上げのみで算出していたため、境界セルが入る/入らないの離散化誤差がそのまま出ていた。実測: g=1mで+0.24%(+7㎡)、g=2mで-0.73%、g=4mで-1.57%(-45㎡≒13.6坪)。敷地が広い物件では自動で格子が粗くなる仕様があるため誤差が拡大しうる状態だった。監査で発見(ユーザー指摘の「平坦度は大丈夫か」に対する全数検証) | e434b0f | #実装 @jissou グローバル:済(「離散的なサンプリングで連続量を測る時、正確な総量が別に分かっているなら総量は正本から取り、サンプルは内訳の比率だけを担わせる」。DEM・ラスタ・格子法一般に効く) ヨコテン:済5件(tallyCutFill/tallyFlat/priceSimUsableArea/bestRefForRegion/DE分割の表示面積＝面積を出す全経路を regionAreaScale() 経由に統一。断面図系は距離ベースで面積を出さないため対象外)
- 2026-08-18 | 委任ワーカーが「commit済み」と報告したが、実際はpushされておらず本番(GitHub Pages)に反映されていなかった。腱さんが本番URLで確認して発覚（「XYが入ってない、古い画面のままだ」） | 原因: ワーカーの完了報告に「commit `5499ef3`」等commitハッシュの記載はあったが、pushの実行・成功確認の記載が無かった。オーケストレータ側もワーカー報告のcommitハッシュを見て「反映済み」と早合点し、git fetchでリモートの実際の状態を確認せずにユーザーへ完了報告していた | 5499ef3,37b5c8d(遅れてpush) | #実装 @jissou グローバル:済(「委任ワーカー・自分を問わず、完了報告は必ずgit fetch+git log origin/mainの実測で裏取りしてから伝える。ローカルHEADにcommitがあることとpush済みであることは別」という教訓は他プロジェクトの委任フローにも汎用) ヨコテン:済0件(今セッションの委任はこの1件のみ発覚。ただし今後の委任報告は全て裏取りを徹底する)
- 2026-08-18 | 平井物件の区画割線(ABC/XY)およびD/E隣接筆(1170-1・1404-4)の境界線が、地図上で白(#ffffff)の破線として描かれており、明るい下地(標準地図タイル・明るい航空写真)に埋もれて視認できなかった | 原因: drawFlat()内でL.polygonのstroke colorを、下地(タイル)の明るさによらず固定の白(#ffffff)でハードコードしていた。腱さんの指摘（docs/task_de_always_and_layout.md）で発覚 | 723ec62 | #描画 @jissou グローバル:不要(地図タイル上に固定色の境界線・ハイライト線を描く機能はこのプロジェクト固有。他プロジェクトはLeaflet等の地図描画を持たない) ヨコテン:済1件(「L.polygon/L.polylineのstroke colorを下地の明るさを考慮せず固定色でハードコードしている箇所」を棚卸し。selLayerの選択断面ハイライト線(旧1485行付近、崖判定タブ)も同型と判定し同コミットで#ffd166(既存の黄=flatRef基準点マーカーと同色)に修正。他の白(#fff)使用箇所(ルーラー点・崖線端点マーカー等)は塗り潰し色主体の縁取りや独自背景付きラベルで下地に直接依存しないため対象外)
- 2026-08-19 | D区画(1170-1)を公図の反対側(南辺)に描いていた・形も全く違った | 原因: 公図のピンク線を機械抽出せず、色検出の断片から形を推測し、さらに登記面積に合わせるための拡大・一様幅オフセットを重ねて作文した。腱さんの「ピンク線をそのままトレースせよ」を2回無視した | 3cdba8c→(本コミット) | #実装 @jissou ヨコテン:0件 グローバル:済(kaihatsu-policy/NOTES.mdへ起票。CLAUDE.md常駐は腱さんの承認待ち)
- 2026-08-19 | D/Eのベクトル化を3回試みて3回とも腱さんに「全然違う」と却下される（形の推測→面積合わせの作文→機械抽出だが細長い帯の南半分を欠落） | 原因: 検証を「実測点との距離」等の数値だけで済ませ、腱さんが実際に見る絵（公図に重ねた形）そのものを自分の目で並べて確認する一手間を毎回省いた。3回目はモルフォロジー処理の閉領域抽出が細く長い帯を分断・部分消失させる弱点に気づけなかった | 062944b→(本コミット) | #実装 @jissou ヨコテン:0件 グローバル:済(kaihatsu-policy/NOTES.mdへ追記＝「機械抽出でも出力形状を必ず目視で原図に重ねて確認してから提示する。数値検証だけで済ませない」)
- 2026-08-19 | D/Eベクトル化撤退に伴い、前提が消えた指示書docs/task_de_independent_adjust.md（DE個別位置調整機能・未実装のまま放置）が残っていた | 原因: 実装を中断(TaskStop)した時点で削除せず、後続のD/E撤退判断でこのファイルの存在を見落としかけた（safe-compact点検で発見） | 1a797e7→(本コミット) | #ドキュメント @jissou ヨコテン:0件 グローバル:不要(このプロジェクト固有の一時指示書の後始末)
- 2026-08-19 | 公図オーバーレイに「ノイズだらけのフルスキャン画像」を使い続けていた | 原因: 腱さんが最初から「ピンクの線だけの画像をオブジェクトとして重ねて、位置は俺が合わせる」と要求していたのに、実装は独自に判断してフルスキャン(黒線・手書き注記・グリッド全部入り)を固定配置するだけの機能にしてしまい、しかも回転非対応だった | 9d48193→(本コミット) | #実装 @jissou ヨコテン:0件 グローバル:不要(このプロジェクト固有のUI要求)
- 2026-08-19 | #deAccuracyHintのD/E精度注意文言が、同じコミット内のコメントで「正本はここ1箇所（deAccuracyHintHTML()）」と明記されていたにも関わらず、updateSubAreaToggleUI()側は実際にはそれを呼ばず同内容を直書きしており、古い登記値比較（D344㎡/E227㎡等）がハードコードされたまま動かなくなっていた | 原因: 5499ef3でupdateSubAreaToggleUI()のaccHint設定を実装した際、deAccuracyHintHTML()経由にせずtextContentへ同文言を直書きしてしまい、同じコミットで書いたコメントの設計意図に実装が追随していなかった（コメントとコードの乖離）。D/Eを4回目のベクトル化で復活させ、文言を動的な面積表示へ更新する作業中に発見 | 5499ef3→3128ee1 | #実装 @jissou グローバル:不要(BUGLOG#12で「同じロジックを表示系/描画系で二重に持たない」を既にグローバル化済みの同型パターン、再登録は重複) ヨコテン:済0件(grep '344㎡\|227㎡\|466㎡\|251㎡'と'正本はここ1箇所'でファイル全体を確認、この1箇所ペアのみ)

- 2026-08-19 | D/Eの4回目実装（道路・川くさびモデル＋extent調整UI）も腱さんの意図（ABC北東辺と川・道路に挟まれた帯全体）と食い違い「絶望する」「ほぼこの形で描画することはむりか」と却下された | 原因: 実測座標という手段は正しくなったが「どの範囲を含めるか」の解釈を依然として実装側が推測で決めていた（塗り指定という形でユーザーが範囲そのものを渡すまで4回とも解釈がずれた） | 1d997d2 | #youken @youken ヨコテン:0件（範囲解釈の機構は本件のD/E系列のみ、既に5回目で解消） グローバル:済（kaihatsu-policy NOTES.md 2026-08-19の2件目に「3回失敗したら同じ原資料への手法変更でなく別の情報源への乗り換えを検討」を記録済み。今回さらに「ユーザーに範囲を塗って指定してもらう」という確実な要件聴取手段が有効だったことを同NOTESに追記）

- 2026-08-19 | D/E個別調整(6回目)の指示書は「deSubAreaPolys()を直せば地図描画・平坦度区画別集計・価格シミュ全部に自動伝播する」と明記していたが、実装中に検証すると価格シミュ(priceSimGroups)と平坦度グリッドの再取得判定(flatGridSig)がdeSubAreaPolys()を経由せずABC専用のSITE_XFORM/siteAdjを別経路で直接参照しており、放置すればD/E調整後も「価格シミュのD/E行」「平坦度計算済み後にD/Eだけ動かした場合の再計算」の2箇所だけ古い位置のまま取り残されるところだった | 原因: priceSimGroups()がSITE.subAreaSets.DEの生pts(ローカル座標)をpriceSimUsableArea(ABCのSITE_XFORM経由)/shoelaceArea(無変換)へそのまま渡す設計のままDE分岐だけ追加されていた。flatGridSig()もsiteAdjのみを指紋にしておりD/E位置の変化を検知できなかった。指示書の完了条件「deSubAreaPolys()を直しただけで自動的に追従するはずなので、そのとおりになっているか実機で確認」に従いD/Eを動かして価格シミュ・平坦度を実際に検証した際に自分で発見、同コミットで修正(priceSimUsableAreaDE新設・flatGridSigへdeRegionAdj追加) | 8861afb | #実装 @jissou ヨコテン:済(grep "subAreaSets\.DE"でSITE.subAreaSets.DEを直接参照する全箇所を棚卸しし、上記2箇所以外に生ptsへの直接依存が無いことを確認) グローバル:不要(kaishu-policyの着手前4問①「同じ知識を持つ場所はどこか」は既にグローバル化済みの原則で、今回はその具体例の1つ。新しい教訓は「指示書自身の①への回答（deSubAreaPolys()が唯一の入口、という自己申告）も鵜呑みにせず実機で裏取りする」という細部で、既存原則の延長にとどまり独立した新規登録は見送る)
- 2026-08-19 | 平坦度計算後、D区画・E区画に色（水色=平ら・緑=軽造成等）が表示されず腱さんに「描画されない」と指摘された | 原因: drawFlat()のセル塗り分けがpointInSite()（ABC専用の内外判定）だけを見ており、D/Eのセルは常に「敷地外」扱いの薄い塗り(fillOpacity 0.25)になっていた。ABC上の0.55と比べて実質見えなかった（データ・座標自体は正常でDOM上には描画されていたが視覚的に不可視） | 1d997d2以降 | #jissou @jissou ヨコテン:0件（pointInSite()の他の呼び出し箇所drawCutFillBadges/drawFlatOutlines/drawCutFillOutlinesは敷地全体専用のバッジ・外郭線描画でありD/E拡張は対象外と判断） グローバル:不要(このアプリ固有の「新設した隣接区画を既存の敷地内外判定へ機械的に組み込み忘れる」という単発の設計漏れで、他アプリで再発する一般的な癖ではない)
- 2026-08-19 | (実装中に自分で発見・出荷前に修正) 購入シナリオ「C+D+E」の2つの基準面(FLAT_PLANES)実装で、指示書のコード例どおりclearFlat()へFLAT_PLANESリセット+saveFlatPlanes()を追加すると、switchSite()で他物件を経由してhiraiへ戻るたびに保存済みの低い面/高い面の設定が消える仕様になるところだった | 原因: switchSite()はCURRENT_SITEを新物件へ切替済みの状態でclearFlat()を内部呼び出ししており、clearFlat()内の無条件saveFlatPlanes()が新物件のlocalStorage(kouteisa_flatplanes_<site>)を{low:null,high:null}で上書きしてから、数行後のloadFlatPlanes()がその上書き後の値を読むだけになる順序だった（flatRefが同じ罠を元々clearFlat()の外でloadFlatRef()を個別処理する設計で回避していたのと同型の罠）。自機能の完了条件「物件を切り替えると面の設定が物件別に保持される」を実機で確認する過程（switchSite('hannan')→switchSite('hirai')往復、localStorage直読み）で自分で発見 | (本コミット) | #実装 @jissou ヨコテン:0件（switchSite()内でCURRENT_SITE再代入後に呼ばれる他の関数updateKouzuToggleUI/updateDeExtentUI/updateSubAreaToggleUI/resetCliffPoints/clearBandを全て確認、いずれもCURRENT_SITEキーのlocalStorageへ無条件saveする処理は無く該当なし。1件だけ同型に近い箇所を発見：applyDeSplitPercent(50)は条件付きでsaveFlatRef()を呼ぶが、その時点でFLAT_GRIDは切替前の物件のまま＝新物件の敷地ポリゴンと空間的に重ならず現状のSITES全5物件(相互に1km以上離れている)では発火しない構造的余地に留まる。switchSite()の内部呼び出し順序を変える必要がありこのタスクの対象範囲外のため、修正はせず記録のみ） グローバル:不要(「switchSite()はCURRENT_SITEを新物件へ切替済みの状態で内部クリーンアップ関数を呼ぶ」というこのアプリの物件切替の作り自体が前提のため、他アプリでは前提が異なる)
- 2026-08-19 | 「C+D+Eで最大化」の進捗バナーが約426㎡と表示するのに、直下の2面構成の表は同じ高い面の合計を423㎡と表示し、同じものが1%弱ずれた別の数字に見えた | 原因: バナーがbestUsableAreaForCells()の戻り値(best.area＝どの標高が最大かを選ぶための格子の素の合計＝内部スコア)をそのまま表示用に使っていた。表側は既存のregionAreaScale()で実測図の面積へ正規化しており、土俵が違った | 1c72c8eの直後に修正 | #jissou @sekkei ヨコテン:済0件（best.area/bestUsableAreaForCells()の全呼び出しを棚卸し。optimizeFlatRef()経由のbestRefForRegion()は既にregionAreaScale()で正規化済み、applyDeSplitPercent()経由は面積を表示せず基準点の選択にしか使っていないため、素の値を表示に使っていたのは今回の新規箇所のみ） グローバル:済（「最適化の内部スコアをそのまま画面に出さない（選択用の値と表示用の値を区別する）」をkaihatsu-policy NOTES.mdへ記録）
- 2026-08-19 | 「基準面(低い面/高い面)」機能について、腱さんが実機で「バグってる」「CDEボタン(高い面)を押しても反応していないように見えた」「高い面に切り替えても低い面に近い見た目だった」と指摘し、直前の実装(1c72c8e)を作り直す指示となった | 原因: 再現条件は未特定だが、`optimizePlaneForPurchaseSet()`がグリッドの鮮度(`flatGridSig()`と`FLAT_GRID_SIG`の一致)を確認せず、D/E変更後も古いグリッドのまま最適化してしまう設計上の穴を発見・修正した。`generateFlat()`は鮮度不一致時に自動で再取得するが、`optimizePlaneForPurchaseSet()`はそれを呼んでいなかった(範囲外のセルが評価から漏れ、高い面のはずが低い面に近い値になりうる) | (本コミット) | #実装 @jissou ヨコテン:済1件(`optimizeFlatRef()`＝低い面ボタンに同型の鮮度チェック漏れが無いか確認。こちらは腱さん指示「もう完成した機能、変えない」により意図的に対象外と判断) グローバル:不要(既存の「同じロジックを複数箇所に二重に持たない」原則(2026-08-17記帳分)の具体例＝新しい消費者(optimizePlaneForPurchaseSet)が既存の鮮度契約(flatGridSig照合)を継承し忘れた、という同型パターンのため独立登録は見送る)
- 2026-08-20 | 平井物件のD/E（隣接2筆）を「面積で指定」欄で個別調整（D=466㎡・E=251㎡）すると、「平坦度を計算」がリロードのたびに事前キャッシュを使わずライブAPI取得（約11,430点、数分）になった。腱さんが実機で「リロードのたびに11284点の測定がだるい。敷地は確定なんだから保存して毎回計らせないで」と苛立ちを伴い指摘 | 原因: `generateFlat()`の事前キャッシュ判定が、今必要な格子(cols/rows/eMin/nMin)とキャッシュの値の完全一致を要求していた。D/E拡張を含む必要範囲がキャッシュ生成時（既定サイズD=410㎡/E=193㎡）の範囲からわずかにズレると不一致になり、`fetch_elev_cache.py`側もD/Eの範囲算出に安全マージンを持っていなかった | (本コミット) | #実装 @jissou ヨコテン:済0件(キャッシュ判定は`generateFlat()`のこの1箇所のみ・複製なしと確認。指示書`docs/task_elev_cache_robust.md`内で回答済み) グローバル:済(「範囲ベースの事前キャッシュは完全一致でなく包含判定にし、生成側に安全マージンを持たせる」という設計パターンをretroスキルで判定。`sekkei-policy/references/architecture.md`への追記に値すると判断し追記案を用意したが、スキルファイルの変更はretroスキル自身の停止条件のためワーカーは実行せず、オーケストレータへの報告に追記案を含めて反映待ち)
- 2026-08-20 | 「高い面」自動選択ボタン（購入シナリオC+D+E）が、腱さんが実際に意図した「敷地左上の道路に近い本当の上の段」ではなく、C+D+E全体からグローバルに使える面積が最大になる段（23.4m／約423㎡）を機械的に選んでいた。腱さんが実機で「計算は合ってるかもしれないけど、欲しいデータにならなかった」と指摘 | 原因: `optimizePlaneForPurchaseSet()`の探索範囲を常に「C+D+E全セル」に固定しており、標高分布が複数の段（プラトー）に分かれる場合にどの段を対象にするかをユーザーが指定する手段が無かった。アルゴリズム（面積最大化）自体は正しく動作していたが、「どの段を最大化すべきか」という要件を確認せず実装側が「常に全体から最大」と仮定していた | (本コミット) | #youken @youken ヨコテン:0件（グローバル探索を絞り込む機構はこのアプリのこの1機能のみ） グローバル:不要（このアプリ固有の探索ロジックの話）
- 2026-08-20 | D/E注意書き(`deAccuracyHintHTML()`)が画面に2回重複表示されていた（`updateSubAreaToggleUI()`内`#deAccuracyHint`への描画と、結果パネル内「隣接筆（D/E）」見出し直前の描画が並存） | 原因: BUGLOG#20（2026-08-19、直書きを`deAccuracyHintHTML()`経由へ修正した回）で文言の出典は統一したが、呼び出し箇所自体が2つ残っていたまま気づかれずにいた。腱さんが実機で指摘 | (本コミット) | #実装 @jissou ヨコテン:済3件(`deAccuracyHintHTML()`の呼び出し箇所を全数確認=`updateSubAreaToggleUI()`/`computeFlatAreasLegacy()`/`computeFlatAreasCutFill()`の3箇所。前者を削除、後2つはFLAT_SCHEME定数で片方のみ到達する同一パスのため重複ではなく1箇所描画と判定・変更なし) グローバル:不要(指示書指定どおり、このアプリ固有のHTML二重描画のため)
- 2026-08-20 | (実装中に自分で発見・出荷前に修正) 「敷地全体」チェックボックス表・価格シミュレーションの「使える面積比」が、低い面を自動選択した直後も常に0㎡のまま表示された（数値ロジック自体は正しく、手動で`regionUsableAreaSelected()`を直接呼ぶと正しい値が返ることを実測で確認） | 原因: `optimizeFlatRef()`が`FLAT_PLANES.low`への代入を`recomputeFlatOnly()`（内部で`drawFlat()`→`computeFlatAreas()`まで実行し、その中で新設のチェックボックス表・価格シミュレーションが`FLAT_PLANES.low`を参照する）より後に行っていたため、初回描画時は`FLAT_PLANES.low`がまだnullだった。指示書は当該箇所を「無変更」と指定していたが、新設ロジックの依存関係と既存の代入順序が噛み合っていなかった。完了条件の実機確認中に発見 | (本コミット) | #実装 @jissou ヨコテン:済1件(高い面側`optimizePlaneForPurchaseSet()`は`FLAT_PLANES.high`設定→`activatePlane('high')`の順で元々問題ないことを確認) グローバル:不要(BUGLOG#28と同型＝新しい消費者が既存関数の暗黙の実行順序契約を継承し忘れたパターンの具体例、独立登録は見送る)
- 2026-08-20 | (実装中に自分で発見・出荷前に修正) 区画割案をXYへ切り替えると、「敷地全体」チェックボックス表でX区画ではなくY区画が既定チェック（主役扱い）になった | 原因: `defaultAreaSelectState()`の主役判定を「案の配列の最後の要素」で行っていたが、ABC配列は`[A,B,C]`で末尾=Cのため正しい一方、XY配列は`applyDeSplitPercent()`実行後は`[X区画,Y区画]`（Xが"主役＝先頭"という`build_split2.py`由来の設計で先頭に来る）に並ぶため、末尾判定だとY区画が誤って主役になる。指示書のコード例が「両方とも最後の要素」という前提を明記していたが、実行時の動的な並び替えまでは検証されていなかった。区画割案トグルの実機確認中に`areaSelectState`を直接見て発見 | (本コミット) | #実装 @jissou ヨコテン:0件(主役判定はABC/XYの2案のみ・この1関数だけの機構) グローバル:不要(「配列の物理位置に意味を持たせると、別関数がその配列を並び替えた時に壊れる」は一般論だが、このアプリのXY動的生成という局所事情との組み合わせでの発現のため独立登録は見送る)
- 2026-08-20 | 平坦度パネル大改修(d0bcb89)後、「使える面積比の試算」が常に「基準区画(A)の使える面積が0のため試算できません」と表示されて機能しなかった | 原因: 基準区画(A)の使える面積計算をregionUsableAreaSelected()（「敷地全体」チェックボックスの選択状態依存）に統一したが、既定でA区画は非チェック（主役はC/X）のため常に0になる設計漏れ。基準区画は「実売価格が分かっている参照点」であって「購入予定の区画セット」ではなく、チェック状態に左右されるべきではなかった | d0bcb89の直後に修正 | #sekkei @jissou ヨコテン:0件（basUsable計算はこの1箇所のみ） グローバル:不要(このアプリ固有の価格シミュ設計の話)

- 2026-08-20 | 「使える面積を自動選択」で区画割案(ABC/XY)の個別区画ラジオ(Xを最大化/Yを最大化等)を選んでも、腱さんには「地図・基準標高が変わらない、敷地全体を選んだ時と同じ結果になる」と見えた | 原因: `bestRefForRegion()`/`optimizePlaneForPurchaseSet()`が、対象区画で絞り込んだセル集合から目標標高(best.elev)を正しく算出した後、その標高に最も近い「実在の標高点」を探す最終ステップだけ絞り込み前の`FLAT_GRID.cells`（敷地全体・D/E・周辺含む全グリッド）を再スキャンしていた。DEMが13300セル中213種類しか無い離散値(21.6mが279回・21.5mが275回等)のため複数区画が同じ目標標高に収束しやすく、その場合マーカーが選択区画の外(時に敷地外)の同一地点に固定され「変わっていない」ように見えていた。実機で`bestRefForRegion(A.pts)`と`bestRefForRegion(Y.pts)`が同一lat/lonを返し、かつ`pointInLLPoly`でその点が敷地外(`insideSite:false`)であることを直接検証して特定 | (本コミット) | #実装 @jissou ヨコテン:済2件(`grep "FLAT_GRID.cells.forEach"`で同型の「絞り込み後にFLAT_GRID全体を再スキャンする」箇所を棚卸し。`bestRefForRegion()`/`optimizePlaneForPurchaseSet()`の2箇所が該当し同コミットで修正。`usableAreaOfLL()`/`usableAreaAtElev()`は各セルでpointInLLPoly判定を都度行う設計のため該当なし、`recomputeFlatOnly()`の全セルdh再計算は意図的な全体処理のため対象外) グローバル:済(「絞り込んだ集合から目標値を計算し、その後『実在の値に丸める』ような二段階処理では、丸め処理の探索対象も同じ絞り込み済み集合を使う。全体集合を使うと離散値の衝突で複数の絞り込み結果が同一点に収束し『絞り込みが効いていない』ように見えるバグになる」。kaihatsu-policy/NOTES.mdへの追記案をオーケストレータへの報告に含める。retroスキル自体の実行・NOTES反映はワーカーでは行わず反映待ち＝BUGLOG#29(2026-08-20の1件目)と同じ扱い)
- 2026-08-20 | (実装中に自分で発見・出荷前に修正) 「高い面」の目標標高を数値で直接指定(setCdeAnchorByElev)する新機能で、ページを再読み込みすると数値指定のcdeAnchorが消えて再指定が必要になるところだった | 原因: `loadCdeAnchor()`の有効判定が`j.lat!=null`（地図クリック由来を前提にlatの有無で判定）のままで、数値直接指定はlat=nullで保存されるため、保存はできてもリロード時に無効判定され読み込まれなかった。新機能の完了確認（リロード後の永続化）を実機で検証中に発見 | (本コミット) | #実装 @jissou ヨコテン:0件(cdeAnchorの永続判定はloadCdeAnchor()のこの1箇所のみ) グローバル:不要(このアプリ固有のcdeAnchorデータ構造の話)
- 2026-08-20 | 価格シミュ「使える面積比」試算で、C区画の試算価格が「面積比」試算価格より大幅に高く出た（腱さん実機報告「Cが1722.7万　違うでしょ　まず単純面積で1138万　Ａとの使える面積比だと　それよりやすくなるでしょＣが」）。使える面積は生面積の部分集合のため通常は面積比より安くなるはずという前提が崩れていた | 原因: `renderPriceSim()`の`baseUsable`（基準区画A自身の使える面積）を、その時たまたま`FLAT_PLANES.low`に入っている標高（直近で「低い面」ボタン経由でどの区画向けに最適化されたか任意＝敷地全体かもしれないしC単独かもしれない）で評価しており、その標高がA区画自身の最適解と一致しない場合にAの使える面積が過小評価され、単価(`unit2=effPrice/baseUsable`)が異常高騰していた。実機再現：`FLAT_PLANES.low`をC区画最大化(22.0m)にした状態で旧ロジックのbaseUsableは420㎡（Aの本来の最良値595㎡より過小）となり、Cの試算価格1717万円（腱さん報告の1722.7万円とほぼ一致）を再現できた。`bestRefForRegion(base.pts, thM)`でA区画専用の最良使える面積を都度計算する方式に修正した結果、baseUsableは`FLAT_PLANES.low`の値に関わらず常に595㎡で安定し、Cの試算価格は1212万円に是正された（面積比1138万円との残差6.5%は、Cの使える面積割合63.4%がAの使える面積割合59.5%より高いという地形的事実を反映した正しい結果と判定。判定根拠：使える面積比price/面積比price = (baseArea/baseUsable)×(C_usable/C_raw)で、A側の生/使える比1.681に対しC側の生/使える比1.579が下回っているため） | (本コミット) | #実装 @jissou ヨコテン:済0件（`unit2`は`renderPriceSim()`内の1変数を全区画(A/B/C/D/E/X/Y)が共有しているため、baseUsableの修正1箇所だけで他区画も同じ乗数で自動的に補正されることを修正前後の実測で確認＝B/D/Eそれぞれの試算価格もCと同じ比率(1.4166倍)で下がっていることを確認した。個別修正は不要） グローバル:不要（このアプリ固有の価格シミュ設計の話。「複数の消費者が共有するグローバル状態(FLAT_PLANES.low)に、特定の消費者専用の計算を依存させない」という教訓自体はBUGLOG 2026-08-20の1件目「基準区画(A)の使える面積計算をチェックボックス状態から独立させる」と同型のため独立登録は見送る)
- 2026-08-20 | 価格シミュ「使える面積比」のD/E区画の坪数が、区画別内訳カードでD/Eそれぞれに表示される「土工まで(50cm)の累計坪」と一致しない（腱さん報告「まずDEの坪数が上の区画ごとの表の『土工までの累計坪』になってない」） | 原因: 区画別内訳カード（`computeFlatAreasCutFill()`のdes.forEach）が`tallyCutFill()`（＝現在アクティブな面`ACTIVE_PLANE`のセル`dh`のみを見る）を使っており、`areaSelectState`（「敷地全体」チェックボックスの選択状態＝どの段(低い面/高い面)を見るかの正本）を無視していた。D/Eの既定チェックは`{low:false,high:true}`で「現在アクティブな面」とは独立に選べるため、ACTIVE_PLANEが'low'の時にD/Eカードは低い面基準の数字を出す一方、価格シミュの使える面積比行(`regionUsableAreaSelected()`)は`areaSelectState`どおり高い面基準で計算しており、両者が食い違っていた。実機再現：ACTIVE_PLANE='low'(22.0m)・D/Eは`high`のみチェック(23.3m)の状態で、旧実装のDカード累計坪=9.3坪、価格シミュのD使える面積=23.3坪と大きく食い違うことを確認 | (本コミット) | #実装 @jissou ヨコテン:済0件（区画別内訳カードでareaSelectStateを無視して`tallyCutFill()`を使っている箇所は`des.forEach`のD/Eカードのみ。ABC/XYの`subs.forEach`も同じ`tallyCutFill()`を使っているが、こちらは意図的に「現在アクティブな面」を見せる仕様として今回のタスク指示書が対象外としており、今回は変更していない） グローバル:不要（このアプリ固有の価格シミュ設計の話。「同じ値を表示する2箇所が別の状態(現在アクティブな面 vs 選択された段の集合)を参照していると数字が食い違う」という教訓自体は既存のBUGLOG#12「同じロジックを表示系/描画系で二重に持たない」と同型のため独立登録は見送る)
- 2026-08-20 | 地図上の30/50/100cm境界線（緑・黒の点線／太線）がD/Eを含んでいない。腱さんがD/EをC区画と隙間なく(flush)調整し直したのでC+D+Eを1つの連続した敷地として境界線を引けるはずが引けていなかった（腱さん指摘「30cm50cm両方のエリア＝つかえるえりあを しめす緑の点線がDとEをふくめた敷地全体で描画できるようにして」） | 原因: `drawCutFillOutlines()`の`inSiteArr`が`pointInSite()`（ABC敷地のみ）だけで内外判定しており、D/E（隣接2筆）のセルは常に「敷地外」として境界線マスクの対象から除外されていた。`drawFlat()`のセル塗り分け（2026-08-19修正・既存BUGLOG参照）は既にD/Eを含める判定に直っていたが、境界線描画（本関数）は当時「敷地全体専用のバッジ・外郭線描画でありD/E拡張は対象外」と判断されたまま据え置かれていた。実機再現：C区画とD区画は幾何的に格子隣接する(4隣接ペア確認)が、修正前は`pointInSite()||D/EのpointInLLPoly()`のOR判定が無いため両区画のセルが同じ連結成分になることは構造上不可能だった。実測で当該境界付近の実標高（C側18.3〜18.4m／D側17.8〜17.9m）を確認し、基準標高18.1m・しきい値50cmで判定したところ、修正前は連結成分がC/D間を跨ぐ箇所0件、修正後は2件（境界を跨いで1つながりになった）と、同一条件下で直接の前後比較により修正を確認した | (本コミット) | #実装 @jissou ヨコテン:済0件（`pointInSite()`の他の呼び出し箇所`drawCutFillBadges()`/`drawFlatOutlines()`は、2026-08-19時点のBUGLOGで「敷地全体専用のバッジ・外郭線描画でありD/E拡張は対象外」と判断済みで、`drawFlatOutlines()`は現在`FLAT_SCHEME='cutfill'`固定のため呼ばれない死コードのため対象外、`drawCutFillBadges()`は切土/盛土バッジの配置ロジックで今回のタスク指示書が対象外としており変更していない） グローバル:不要（このアプリ固有の地図描画の話。D/Eを持たない物件(函南・camp・furuya・mikokai)では`deSubAreaPolys()`が空配列を返し`.some()`が常にfalseになるため副作用が無いことも実機で確認済み）
- 2026-08-20 | (実装中に自分で発見・出荷前に修正) 項目4のブロック再編で新設した「敷地全体」セクションの「合計」カードが、外枠と全く同じ背景色/枠線色になり境界が視認できなくなるところだった | 原因: `regionCardHTML(name,body,isTotal)`のisTotal=true時の色(`border-left:#3b82f6;background:#132436`)を、新設の外枠コンテナ（同じく`background:#132436;border:1px solid #3b82f6`＝「これは合算値」を示すために選んだ色）の中で「合計」カードにも使ったため、カードの背景が親要素の背景と完全に一致し、カード自身の境界線（left-borderのみ）以外は視覚的に同化していた。実機のDOM出力（`background:#132436`が親子で連続して2回出現）を確認して発見 | (本コミット) | #描画 @jissou ヨコテン:0件（isTotal色を使う`regionCardHTML()`呼び出しはこの1箇所のみ新設） グローバル:不要（このアプリ固有の色設計の話。「入れ子要素に同じ強調色を重ねて使うと境界が消える」という教訓自体は一般的だが、今回は単発の実装ミスで再発防止の仕組み化が必要なほどの頻度ではないと判断）
