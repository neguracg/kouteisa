# WORKLOG

## 2026-07-24
- 2026-07-24 平坦度判定機能を実装（がけ判定と兄弟タブ）。index.htmlにタブUI・基準点機能・generateFlat/drawFlat/marching squares等価数・面積集計を追加。次予定：オーケストレータによるレビュー・コミット。
- 2026-07-24 実機検証完了（python -m http.server 8934、静的サーバーは検証後停止済み）。がけ判定の断面図作成・2H帯生成・共有URL(VIEWモード)の回帰なしを確認。平坦度モード（基準点設置→計算→色分け・等高線・3分類面積）を確認。setTool()のlocalStorage上書きバグを発見し即修正。次予定：なし（実装完了）。
- 2026-07-24 公図(kashiya_kouzu.PDF)から地番1230-2+1227-1合体トレースでSITES.mikokaiを更新（rect:true矩形→rect:false・21頂点・targetArea628→657）。名称を「未公開 物件（柏谷・公図トレース199坪）」に変更。手法はPyMuPDFで300dpiラスタ化→flood fill(壁=黒線)→Moore境界トレース→Douglas-Peucker(eps=5)。地図の座標グリッド枠(125.000m×126.000m)から独立検算したpx/m換算では1230-2≈274㎡・1227-1≈277㎡・合計≈553㎡で、指示書の想定比371:286(計657)とは一致せず（比0.99 vs 1.30、総額19%差）。ただし視覚照合（ラベル位置・境界形状）で筆の取り違えではないことは確認済み。指示通りtargetArea:657を採用したが、371/286の出典（登記簿等）を要確認とオーケストレータに報告。次予定：オーケストレータが371/286の出典確認、または653台への修正要否を判断。
- 2026-07-24 機能1（断面図の任意区間角度測定）実装。updateReadout()にpicks0/1/2点いずれでも「全体勾配（始点→終点）」を常時表示、picks2点時の表示を「選択2点間 勾配」に文言明確化。drawPicks()にpicks2点を結ぶ細線(#7ee0a3破線)＋角度ラベルをSVG追加（H・2Hラベルとは別位置＝xMid,yMidH-18）。ブラウザJS実行で全ケース（0/1/2点）のHTML出力とSVG line要素の座標を直接検証、期待通り。
- 2026-07-24 機能2（平坦度エリアの枠付き描画）実装。gridComponents()（4連結成分）＋traceMaskOutline()（辺追跡→端点一致で閉ループ化）を追加。computeFlatAreas()をareas値を返すようリファクタし、drawFlatOutlines()が同じ値を使ってツールチップに表示＝面積の不一致が起きない設計。白枠(weight3,#ffffff,fill緑#3ecf7e op.18)＝そのまま平ら、橙破線(weight2,#ff9d1a)＝造成対応(平らを含む外側輪郭)。1〜2セルの飛び地は輪郭省略（面積合計には算入）。flatResultに凡例1行追加。mikokaiで実行しwhiteOutlineCount=2/orangeOutlineCount=2、ツールチップの面積(136㎡/16㎡)がcomputeFlatAreasの表示値と完全一致を確認。1セル(4㎡)の飛び地が輪郭なしで面積のみ算入されるケースも確認。
- 2026-07-24 機能3（等高線の途切れ）を実機再現→原因確定→修正。**指示書の仮説(DEM5mメッシュ)は誤り**：実際にAPIの`hsrc`フィールドを取得するとmikokai敷地全域で「1m（レーザ）」＝1mメッシュだった。真因は「1m解像度の実地形の細かい凹凸を2m格子でサンプリングした際のエイリアシング」で、marching squaresが検出する交点が隣接セルと繋がらない孤立セグメントになる現象（ヌルセル・鞍点(4交点)は0件で無関係と確認済み）。ブラウザJSで直接chain連結解析：生データは11レベルで92チェイン中70が孤立(76%)。3x3平滑化を1〜4パス・5x5幅でスイープした結果**1パスが最良**（92→62チェイン、70→45孤立）で2パス以上はレベルによって悪化（指示書の「1〜2パス」から1パスのみに絞った理由）。smoothDhGrid()（等高線専用、cells本体は不変）とjoinSegments()（端点一致でポリライン化、marching squares独立描画の解消）を実装。実データで944個の独立ポリライン→191個(うち45個は最大34点の連結チェイン)に削減したことを確認。before/afterスクリーンショットでも視覚的な滑らかさ改善を確認。
- 2026-07-24 機能4（調整用の参照地番）実装。Pythonで1228-16,1228-5,1228-12,1228-11,1228-6(道路)を公図からトレース（_input/kashiya/trace_refs.py新規）。メイン敷地(1230-2+1227-1)を旧crop窓(X0=1150,Y0=1350)で再トレースしcx_old=1040.9524,cy_old=487.9524を復元（index.html現行shapeとの差分0.0で検証済み）→フルページ原点FPX=2190.952,FPY=1837.952を算出→新crop窓(500,1650,1520,3276)でトレースした5筆をこの原点基準に変換。1228-6(道路)は南端が隣接1228-1との境界が薄く素朴floodでは漏れる不具合を発見、Y1=3276で物理的にクロップして解消（診断: 幅135px一定→3278px地点から235pxまで急増で確認）。px面積: 1228-16=116166px(208.4㎡・6頂点), 1228-5=125794px(225.7㎡・6頂点), 1228-12=99908px(179.2㎡・8頂点), 1228-11=84412px(151.4㎡・13頂点), 1228-6=137010px(245.8㎡・13頂点、南端打ち切りのため実際はやや過小)。種点(フルページpx座標): 1228-16=(1300,1950), 1228-5=(800,1950), 1228-12=(900,2100), 1228-11=(1300,2080), 1228-6=(1190,2400)+(1230,2950)+(1180,2320)+(1220,3150)の4点（連結性は確認済みで実際は1点でも足りたが安全のため複数打ち）。index.html側はbuildSite()をshapeToLatLngs(pts,xf)ヘルパーにリファクタ（xf=メインshapeの重心cx,cy・倍率s・回転cr,sr・原点lat0,lon0・m換算mLat,mLonを1箇所で算出）し、refShapesにも同じxfを渡すことで相対位置・相対サイズを保持。siteRefLayerはsiteAdjustMode中のみmapに追加、道路(1228-6)のみ#5ab0ff。ブラウザJSで5筆の描画・rotateSite/scaleSite追随・adjust ON/OFF・hannan等他物件での無影響を直接検証済み。overlay画像で公図の実際の境界線と1px単位で一致することも視覚確認済み。
- 2026-07-24 全機能の実機検証完了（python -m http.server 8934、検証後にサーバー停止済み）。構文チェック(node --check相当)OK、コンソールエラーなし。既存機能の無回帰確認：物件切替4件、がけ判定の断面図作成・2H帯生成(hannan・35断面)、共有URL(クエリ変更で強制リロードしVIEWモード再現・BAND_STRIPS復元を確認)。次予定：オーケストレータによるレビュー・コミット。
